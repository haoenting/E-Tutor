#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main() {
	char arr[10005]; // 定義字串array
	while (scanf("%s", arr) != EOF) { // 輸入直到EOF
		for (int i = strlen(arr); i > 0; i--) { // 抓取字元數，從最後一個往前
			printf("%c", arr[i - 1]); //印出字元
		}
		printf("\n");
	}
	return 0;
}
